name: Update Readme Content

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build & Keep Readme Fresh
        run: |
          echo "Automated Readme Pipeline executed cleanly."
